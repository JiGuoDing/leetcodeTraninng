#include <iostream>
#include <stack>
using namespace std;
string simplifyPath(string path) {
    stack<string> str_stk;
    string simplifiedPath = "", dir = "";
    for(auto ch : path){
        if((ch >= 'A' && ch <= 'Z') || (ch >= 'a' && ch <= 'z') || (ch >= '0' && ch <= '9'))
            dir += ch;
        else if ()
    }
}
int main(){

    return 0;
}